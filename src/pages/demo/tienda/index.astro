---
/**
 * Tienda GENÉRICA — la del recorrido transaccional completo (rutas históricas,
 * enlazadas desde la landing, /arquitectura y /dossier). Colección fija; las 8
 * del escaparate de estilos viven en /demo/tiendas/<id>/ con esta MISMA página.
 */
import CatalogPage from '../../../components/store/CatalogPage.astro';
import { defaultCollection } from '../../../lib/collections';

export const prerender = false;
---

<CatalogPage collection={defaultCollection()} db={Astro.locals.runtime.env.DB} url={Astro.url} />
