---
import CartPage from '../../components/store/CartPage.astro';
import { defaultCollection } from '../../lib/collections';

export const prerender = false;
---

<CartPage collection={defaultCollection()} />
