---
import type { CollectionConfig, StorePaths } from '../../../collections';

interface Props {
  paths: StorePaths;
  collection: CollectionConfig;
  count: number;
  activeCategory?: string | undefined;
  sort: string;
  search?: string | undefined;
}

const { paths, collection, count, activeCategory, sort, search } = Astro.props;
const hrefFor = (category?: string) => {
  const params = new URLSearchParams();
  if (category) params.set('categoria', category);
  if (sort !== 'featured') params.set('orden', sort);
  if (search) params.set('q', search);
  const query = params.toString();
  return `${paths.catalog}${query ? `?${query}` : ''}`;
};
---

<div class="sillage-filters">
  <nav aria-label="Categorías">
    <span>Catálogo ({count})</span>
    <a href={hrefFor()} aria-current={!activeCategory ? 'page' : undefined}>Todo</a>
    {collection.categories.map((category) => (
      <a href={hrefFor(category.id)} aria-current={activeCategory === category.id ? 'page' : undefined}>{category.label}</a>
    ))}
  </nav>
  <div class="sillage-filters__forms">
    <form method="get" action={paths.catalog} role="search">
      {activeCategory && <input type="hidden" name="categoria" value={activeCategory} />}
      {sort !== 'featured' && <input type="hidden" name="orden" value={sort} />}
      <input type="search" name="q" value={search} maxlength="60" placeholder="Buscar" aria-label="Buscar productos" />
    </form>
    <form method="get" action={paths.catalog}>
      {activeCategory && <input type="hidden" name="categoria" value={activeCategory} />}
      {search && <input type="hidden" name="q" value={search} />}
      <select name="orden" aria-label="Ordenar catálogo" onchange="this.form.submit()">
        <option value="featured" selected={sort === 'featured'}>Destacados</option>
        <option value="price-asc" selected={sort === 'price-asc'}>Precio ↑</option>
        <option value="price-desc" selected={sort === 'price-desc'}>Precio ↓</option>
        <option value="name" selected={sort === 'name'}>Nombre A–Z</option>
      </select>
    </form>
  </div>
</div>

<style>
  .sillage-filters { display: flex; align-items: center; justify-content: space-between; gap: 1rem; margin-bottom: 2rem; padding-block: .9rem; border-block: 1px solid var(--color-border); color: var(--color-foreground); font-size: .68rem; }
  .sillage-filters nav, .sillage-filters__forms { display: flex; align-items: center; gap: .9rem; }
  .sillage-filters nav > span { color: var(--color-muted-foreground); }
  .sillage-filters a[aria-current='page'] { text-decoration: underline; text-underline-offset: .25rem; }
  .sillage-filters input, .sillage-filters select { min-height: 2.35rem; border: 0; border-bottom: 1px solid var(--color-border); border-radius: 0; color: var(--color-foreground); background: transparent; font: inherit; }
  .sillage-filters input { width: 7.5rem; }

  @media (max-width: 700px) {
    .sillage-filters { align-items: flex-start; flex-direction: column; overflow: hidden; }
    .sillage-filters nav { width: 100%; padding-bottom: .4rem; overflow-x: auto; }
    .sillage-filters nav > * { flex: 0 0 auto; }
    .sillage-filters__forms { width: 100%; justify-content: space-between; }
  }
</style>
