---
/**
 * MINIMAL · Catalog — vista completa del catálogo para el tema, montada por la
 * página (src/pages/demo/tienda/index.astro) a través del registro de temas.
 * Barra `Catalog (N) · Ordenar · categorías` + rejilla de 2 columnas con aire.
 */
import type { CollectionConfig, StorePaths } from '../../../lib/collections';
import Filters from './Filters.astro';
import ProductGrid from './ProductGrid.astro';

type GridProduct = {
  slug: string;
  name: string;
  price_cents: number;
  image: string;
  stock: number;
};

interface Props {
  paths: StorePaths;
  products: GridProduct[];
  collection: CollectionConfig;
  category?: string | undefined;
  sort: string;
  search?: string | undefined;
}

const { paths, products, collection, category, sort, search } = Astro.props;
---

<main class="px-5 py-6 md:px-2 md:py-10">
  <Filters paths={paths} collection={collection} count={products.length} activeCategory={category} sort={sort} search={search} />
  <div class="mt-8">
    {products.length > 0 ? (
      <ProductGrid paths={paths} products={products} />
    ) : (
      <div class="py-20 text-center">
        <p class="font-display text-lg">
          {search ? `Nada por aquí para «${search}»` : 'No hay productos en esta categoría'}
        </p>
        <a
          href={paths.catalog}
          class="mt-4 inline-block text-[11px] uppercase tracking-[0.14em] text-muted-foreground underline underline-offset-4 transition-colors hover:text-foreground"
        >Ver todo el catálogo</a>
      </div>
    )}
  </div>
</main>
